class Player {
  constructor(id, name, avatar, x, y, points) {
    this.id = id;
    this.name = name;
    this.avatar = avatar;
    this.x = x;
    this.y = y;
    this.points = points;
  }
}

module.exports = Player;
